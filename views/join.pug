extends layout/main.pug

block content
    div.form__ontainer
        form(action=routes.join, method="post")
            input(type="name", name="name", placeholder="Full name", require=true)
            input(type="email", name="email", placeholder="Email", require=true)
            input(type="password", name="password", placeholder="Password", require=true)
            input(type="password", name="password2", placeholder="Verify Password", require=true)
            input(type="submit", value="Join Now")
            include partials/socialLogin.pug